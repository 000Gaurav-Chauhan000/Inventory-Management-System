services:
  # Api Gateway (Publicly accessible)
  - type: web
    name: api-gateway
    env: docker
    dockerContext: StockPro
    dockerfilePath: ApiGateway/Dockerfile
    plan: free
    envVars:
      - key: RENDER
        value: "true"
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: ASPNETCORE_URLS
        value: http://+:8080

  # Auth Service (Converted to web for free tier)
  - type: web
    name: auth-service
    env: docker
    dockerContext: StockPro
    dockerfilePath: AuthService/Dockerfile
    plan: free
    envVars:
      - key: ConnectionStrings__DefaultConnection
        sync: false # User should set this in Render UI
      - key: Jwt__Key
        sync: false
      - key: Jwt__Issuer
        value: StockPro
      - key: Jwt__Audience
        value: StockProUsers
      - key: ASPNETCORE_URLS
        value: http://+:8080

  # Product Service (Converted to web for free tier)
  - type: web
    name: product-service
    env: docker
    dockerContext: StockPro
    dockerfilePath: ProductService/Dockerfile
    plan: free
    envVars:
      - key: ConnectionStrings__DefaultConnection
        sync: false
      - key: ASPNETCORE_URLS
        value: http://+:8080

  # Warehouse Service (Converted to web for free tier)
  - type: web
    name: warehouse-service
    env: docker
    dockerContext: StockPro
    dockerfilePath: WarehouseService/Dockerfile
    plan: free
    envVars:
      - key: ConnectionStrings__DefaultConnection
        sync: false
      - key: ASPNETCORE_URLS
        value: http://+:8080

  # Supplier Service (Converted to web for free tier)
  - type: web
    name: supplier-service
    env: docker
    dockerContext: StockPro
    dockerfilePath: SupplierService/Dockerfile
    plan: free
    envVars:
      - key: ConnectionStrings__DefaultConnection
        sync: false
      - key: ASPNETCORE_URLS
        value: http://+:8080

  # Purchase Service (Converted to web for free tier)
  - type: web
    name: purchase-service
    env: docker
    dockerContext: StockPro
    dockerfilePath: PurchaseOrderService/Dockerfile
    plan: free
    envVars:
      - key: ConnectionStrings__DefaultConnection
        sync: false
      - key: ASPNETCORE_URLS
        value: http://+:8080

  # Stock Movement Service (Converted to web for free tier)
  - type: web
    name: movement-service
    env: docker
    dockerContext: StockPro
    dockerfilePath: StockMovementService/Dockerfile
    plan: free
    envVars:
      - key: ConnectionStrings__DefaultConnection
        sync: false
      - key: ASPNETCORE_URLS
        value: http://+:8080

  # Alert Service (Converted to web for free tier)
  - type: web
    name: alert-service
    env: docker
    dockerContext: StockPro
    dockerfilePath: AlertService/Dockerfile
    plan: free
    envVars:
      - key: ConnectionStrings__DefaultConnection
        sync: false
      - key: ASPNETCORE_URLS
        value: http://+:8080

  # Report Service (Converted to web for free tier)
  - type: web
    name: report-service
    env: docker
    dockerContext: StockPro
    dockerfilePath: ReportService/Dockerfile
    plan: free
    envVars:
      - key: ConnectionStrings__DefaultConnection
        sync: false
      - key: ASPNETCORE_URLS
        value: http://+:8080
